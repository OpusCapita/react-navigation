import React from 'react';
import { expect } from 'chai';
import { shallow } from 'enzyme';
import MenuDropdownGrid from '.';

describe('<MenuDropdownGrid />', () => {
  /* Recommended test-cases

  it('should have default props', () => {
    let component = <MenuDropdownGrid />;
    expect(component.props.testProp).to.equal('Give me back my label!');
    expect(component.props.onClick).to.be.a('function');
  });
  it('should have the right class name', () => {
    let wrapper = shallow(<MenuDropdownGrid className="test-class-name" />);
    expect(wrapper).to.have.className('menu-dropdown-grid');
    expect(wrapper).to.have.className('test-class-name');
  });

  */
});
