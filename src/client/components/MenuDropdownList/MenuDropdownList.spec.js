import React from 'react';
import { expect } from 'chai';
import { shallow } from 'enzyme';
import MenuDropdownList from '.';

describe('<MenuDropdownList />', () => {
  /* Recommended test-cases

  it('should have default props', () => {
    let component = <MenuDropdownList />;
    expect(component.props.testProp).to.equal('Give me back my label!');
    expect(component.props.onClick).to.be.a('function');
  });
  it('should have the right class name', () => {
    let wrapper = shallow(<MenuDropdownList className="test-class-name" />);
    expect(wrapper).to.have.className('menu-dropdown-list');
    expect(wrapper).to.have.className('test-class-name');
  });

  */
});
